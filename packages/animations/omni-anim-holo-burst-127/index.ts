import { OmniWebMotionEngine } from '@omni-bridge/ui/motion';

export const omni_anim_holo_burst_127 = () => {
    return {
        id: "omni-anim-holo-burst-127",
        duration: 272,
        easing: "omni-cyberpunk-glitch",
        execute: (el: HTMLElement) => {
            el.style.transform = "translate3d(32px, -44px, 0)";
            el.style.filter = "contrast(1.6358355878592596) hue-rotate(88deg)";
        }
    };
};
