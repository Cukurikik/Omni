import { OmniWebMotionEngine } from '@omni-bridge/ui/motion';

export const omni_anim_matrix_bounce_176 = () => {
    return {
        id: "omni-anim-matrix-bounce-176",
        duration: 504,
        easing: "omni-cyberpunk-glitch",
        execute: (el: HTMLElement) => {
            el.style.transform = "translate3d(-2px, 45px, 0)";
            el.style.filter = "contrast(1.5441566998884921) hue-rotate(180deg)";
        }
    };
};
