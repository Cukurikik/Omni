import { OmniWebMotionEngine } from '@omni-bridge/ui/motion';

export const omni_anim_neon_bounce_042 = () => {
    return {
        id: "omni-anim-neon-bounce-042",
        duration: 1355,
        easing: "omni-cyberpunk-glitch",
        execute: (el: HTMLElement) => {
            el.style.transform = "translate3d(-35px, -25px, 0)";
            el.style.filter = "contrast(1.0587476579608033) hue-rotate(168deg)";
        }
    };
};
