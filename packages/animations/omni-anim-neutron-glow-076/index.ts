import { OmniWebMotionEngine } from '@omni-bridge/ui/motion';

export const omni_anim_neutron_glow_076 = () => {
    return {
        id: "omni-anim-neutron-glow-076",
        duration: 790,
        easing: "omni-cyberpunk-glitch",
        execute: (el: HTMLElement) => {
            el.style.transform = "translate3d(-11px, 5px, 0)";
            el.style.filter = "contrast(2.3240287556172237) hue-rotate(291deg)";
        }
    };
};
