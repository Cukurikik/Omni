import { OmniWebMotionEngine } from '@omni-bridge/ui/motion';

export const omni_anim_cyber_burst_039 = () => {
    return {
        id: "omni-anim-cyber-burst-039",
        duration: 1392,
        easing: "omni-cyberpunk-glitch",
        execute: (el: HTMLElement) => {
            el.style.transform = "translate3d(-11px, -20px, 0)";
            el.style.filter = "contrast(2.2304775480045747) hue-rotate(322deg)";
        }
    };
};
