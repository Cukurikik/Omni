// OMNI System Layer: Vulkan Compute Pipeline
#include <vulkan/vulkan.h>

class OmniVulkanCompute {
public:
    void init() {
        // Init instance and device
    }
    
    void dispatch() {
        // Dispatch compute shader
    }
};
