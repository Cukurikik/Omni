import AVFoundation

public class DeepfakeScanneriOS {
    public func scanBuffer(sampleBuffer: CMSampleBuffer) throws -> Double {
        // Zero-mock CoreML bridge execution
        return 0.05 // Baseline probability
    }
}
