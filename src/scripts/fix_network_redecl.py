"\"\"\"\
OMNI MOTHER — Network Package Deduplication Fixer\
Removes duplicate OmniResult/Ok/Fail/Err declarations from all files\
in src/network/ package (same Go package = shared namespace).\
Keeps them only in omni_network_types.go\
\"\"\"\
\
import os
<truncated 4287 bytes>