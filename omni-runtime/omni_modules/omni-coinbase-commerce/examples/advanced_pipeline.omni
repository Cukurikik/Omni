// Example: Advanced pipeline with omni-coinbase-commerce
import { DataPipeline } from '@omni/omni-coinbase-commerce/compute'
import { TypeSafeClient } from '@omni/omni-coinbase-commerce/ui'

fn main() {
    let pipeline = DataPipeline::new()
        .add_stage('fetch', |_| client.get('/data'))
        .add_stage('transform', |d| d.map(|x| x * 2))
        .add_stage('store', |d| client.post('/results', d))
    pipeline.execute(None)?
}