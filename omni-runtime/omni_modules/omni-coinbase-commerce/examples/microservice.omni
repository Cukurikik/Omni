// Example: Microservice with omni-coinbase-commerce
import { HTTPServer } from '@omni/omni-coinbase-commerce/network'
import { DomainService } from '@omni/omni-coinbase-commerce/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}