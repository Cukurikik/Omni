// Example: Microservice with omni-uuid-native
import { HTTPServer } from '@omni/omni-uuid-native/network'
import { DomainService } from '@omni/omni-uuid-native/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}