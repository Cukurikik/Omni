// Example: Advanced pipeline with omni-pdf-generator
import { DataPipeline } from '@omni/omni-pdf-generator/compute'
import { TypeSafeClient } from '@omni/omni-pdf-generator/ui'

fn main() {
    let pipeline = DataPipeline::new()
        .add_stage('fetch', |_| client.get('/data'))
        .add_stage('transform', |d| d.map(|x| x * 2))
        .add_stage('store', |d| client.post('/results', d))
    pipeline.execute(None)?
}