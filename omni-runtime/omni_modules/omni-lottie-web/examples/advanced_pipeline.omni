// Example: Advanced pipeline with omni-lottie-web
import { DataPipeline } from '@omni/omni-lottie-web/compute'
import { TypeSafeClient } from '@omni/omni-lottie-web/ui'

fn main() {
    let pipeline = DataPipeline::new()
        .add_stage('fetch', |_| client.get('/data'))
        .add_stage('transform', |d| d.map(|x| x * 2))
        .add_stage('store', |d| client.post('/results', d))
    pipeline.execute(None)?
}