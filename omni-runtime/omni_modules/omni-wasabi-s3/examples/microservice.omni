// Example: Microservice with omni-wasabi-s3
import { HTTPServer } from '@omni/omni-wasabi-s3/network'
import { DomainService } from '@omni/omni-wasabi-s3/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}