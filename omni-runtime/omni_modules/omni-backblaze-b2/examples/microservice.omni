// Example: Microservice with omni-backblaze-b2
import { HTTPServer } from '@omni/omni-backblaze-b2/network'
import { DomainService } from '@omni/omni-backblaze-b2/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}