// Example: Advanced pipeline with omni-azure-blob
import { DataPipeline } from '@omni/omni-azure-blob/compute'
import { TypeSafeClient } from '@omni/omni-azure-blob/ui'

fn main() {
    let pipeline = DataPipeline::new()
        .add_stage('fetch', |_| client.get('/data'))
        .add_stage('transform', |d| d.map(|x| x * 2))
        .add_stage('store', |d| client.post('/results', d))
    pipeline.execute(None)?
}