// Example: Microservice with omni-parallax-core
import { HTTPServer } from '@omni/omni-parallax-core/network'
import { DomainService } from '@omni/omni-parallax-core/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}