// Example: Microservice with omni-netlify-edge
import { HTTPServer } from '@omni/omni-netlify-edge/network'
import { DomainService } from '@omni/omni-netlify-edge/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}