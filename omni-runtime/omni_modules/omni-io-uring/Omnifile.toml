[package]
name        = "omni-io-uring"
version     = "1.0.0"
description = "Libuv killer. Native Linux io_uring structures mapping into Go routing channels."
license     = "OMNI-Community"
tier        = "free"

[permissions]
allow_syscall = true
