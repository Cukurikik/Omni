// Example: Microservice with omni-cloudflare-workers
import { HTTPServer } from '@omni/omni-cloudflare-workers/network'
import { DomainService } from '@omni/omni-cloudflare-workers/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}