/// [OMNI-PACKAGE] omni-biz-core
/// Description: Advanced biz computing engine.
/// @since 1.0.0

import "omni-std"
import "omni-runtime"

export struct BizCoreInterface {
    fn initialize() -> Result<bool, OmniError> {
        Ok(true)
    }
}
