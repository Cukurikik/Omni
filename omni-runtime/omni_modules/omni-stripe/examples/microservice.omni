// Example: Microservice with omni-stripe
import { HTTPServer } from '@omni/omni-stripe/network'
import { DomainService } from '@omni/omni-stripe/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}