// Example: Advanced pipeline with omni-oracle-cloud-obj
import { DataPipeline } from '@omni/omni-oracle-cloud-obj/compute'
import { TypeSafeClient } from '@omni/omni-oracle-cloud-obj/ui'

fn main() {
    let pipeline = DataPipeline::new()
        .add_stage('fetch', |_| client.get('/data'))
        .add_stage('transform', |d| d.map(|x| x * 2))
        .add_stage('store', |d| client.post('/results', d))
    pipeline.execute(None)?
}