// Example: Microservice with omni-zod-native
import { HTTPServer } from '@omni/omni-zod-native/network'
import { DomainService } from '@omni/omni-zod-native/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}