// Example: Advanced pipeline with omni-google-pay
import { DataPipeline } from '@omni/omni-google-pay/compute'
import { TypeSafeClient } from '@omni/omni-google-pay/ui'

fn main() {
    let pipeline = DataPipeline::new()
        .add_stage('fetch', |_| client.get('/data'))
        .add_stage('transform', |d| d.map(|x| x * 2))
        .add_stage('store', |d| client.post('/results', d))
    pipeline.execute(None)?
}