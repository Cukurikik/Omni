// Example: Microservice with omni-vultr-obj
import { HTTPServer } from '@omni/omni-vultr-obj/network'
import { DomainService } from '@omni/omni-vultr-obj/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}