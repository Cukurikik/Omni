// Example: Microservice with omni-spline-3d
import { HTTPServer } from '@omni/omni-spline-3d/network'
import { DomainService } from '@omni/omni-spline-3d/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}