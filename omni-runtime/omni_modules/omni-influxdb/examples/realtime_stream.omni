// Example: Realtime streaming with omni-influxdb
import { WSHub } from '@omni/omni-influxdb/network'

fn main() {
    let hub = WSHub::new()
    hub.on_connect(|conn| println!("Connected: {}", conn.id))
    hub.on_message(|msg| hub.broadcast(msg.room, msg.data))
    hub.listen(8080)
}