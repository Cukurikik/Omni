// Example: Microservice with omni-cockroachdb
import { HTTPServer } from '@omni/omni-cockroachdb/network'
import { DomainService } from '@omni/omni-cockroachdb/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}