// Example: Microservice with omni-moment-turbo
import { HTTPServer } from '@omni/omni-moment-turbo/network'
import { DomainService } from '@omni/omni-moment-turbo/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}