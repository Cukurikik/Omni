// Example: Microservice with omni-socket-io-native
import { HTTPServer } from '@omni/omni-socket-io-native/network'
import { DomainService } from '@omni/omni-socket-io-native/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}