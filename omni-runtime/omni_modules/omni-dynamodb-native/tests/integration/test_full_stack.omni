// Integration: Full-stack omni-dynamodb-native connectivity
import { init } from '@omni/omni-dynamodb-native'

test 'full stack boot' {
    let instance = init(Config::from_env())?
    assert instance.driver.is_connected()
    assert instance.pool.available_count() > 0
}