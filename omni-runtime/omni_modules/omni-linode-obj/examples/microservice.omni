// Example: Microservice with omni-linode-obj
import { HTTPServer } from '@omni/omni-linode-obj/network'
import { DomainService } from '@omni/omni-linode-obj/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}