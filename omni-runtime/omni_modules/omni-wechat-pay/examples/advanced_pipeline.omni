// Example: Advanced pipeline with omni-wechat-pay
import { DataPipeline } from '@omni/omni-wechat-pay/compute'
import { TypeSafeClient } from '@omni/omni-wechat-pay/ui'

fn main() {
    let pipeline = DataPipeline::new()
        .add_stage('fetch', |_| client.get('/data'))
        .add_stage('transform', |d| d.map(|x| x * 2))
        .add_stage('store', |d| client.post('/results', d))
    pipeline.execute(None)?
}