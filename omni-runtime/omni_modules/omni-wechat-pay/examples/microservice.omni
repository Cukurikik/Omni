// Example: Microservice with omni-wechat-pay
import { HTTPServer } from '@omni/omni-wechat-pay/network'
import { DomainService } from '@omni/omni-wechat-pay/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}