// Example: Microservice with omni-typed-js
import { HTTPServer } from '@omni/omni-typed-js/network'
import { DomainService } from '@omni/omni-typed-js/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}