// Example: Microservice with omni-heroku-dynos
import { HTTPServer } from '@omni/omni-heroku-dynos/network'
import { DomainService } from '@omni/omni-heroku-dynos/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}