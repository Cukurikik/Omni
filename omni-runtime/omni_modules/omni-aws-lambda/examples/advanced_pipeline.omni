// Example: Advanced pipeline with omni-aws-lambda
import { DataPipeline } from '@omni/omni-aws-lambda/compute'
import { TypeSafeClient } from '@omni/omni-aws-lambda/ui'

fn main() {
    let pipeline = DataPipeline::new()
        .add_stage('fetch', |_| client.get('/data'))
        .add_stage('transform', |d| d.map(|x| x * 2))
        .add_stage('store', |d| client.post('/results', d))
    pipeline.execute(None)?
}