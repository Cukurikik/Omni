// Example: Realtime streaming with omni-razorpay
import { WSHub } from '@omni/omni-razorpay/network'

fn main() {
    let hub = WSHub::new()
    hub.on_connect(|conn| println!("Connected: {}", conn.id))
    hub.on_message(|msg| hub.broadcast(msg.room, msg.data))
    hub.listen(8080)
}