// Example: Realtime streaming with omni-ibm-cloud-obj
import { WSHub } from '@omni/omni-ibm-cloud-obj/network'

fn main() {
    let hub = WSHub::new()
    hub.on_connect(|conn| println!("Connected: {}", conn.id))
    hub.on_message(|msg| hub.broadcast(msg.room, msg.data))
    hub.listen(8080)
}