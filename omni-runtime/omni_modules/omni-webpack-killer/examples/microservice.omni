// Example: Microservice with omni-webpack-killer
import { HTTPServer } from '@omni/omni-webpack-killer/network'
import { DomainService } from '@omni/omni-webpack-killer/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}