// Example: Microservice with omni-websocket-cluster
import { HTTPServer } from '@omni/omni-websocket-cluster/network'
import { DomainService } from '@omni/omni-websocket-cluster/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}