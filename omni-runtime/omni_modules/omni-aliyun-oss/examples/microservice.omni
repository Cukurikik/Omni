// Example: Microservice with omni-aliyun-oss
import { HTTPServer } from '@omni/omni-aliyun-oss/network'
import { DomainService } from '@omni/omni-aliyun-oss/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}