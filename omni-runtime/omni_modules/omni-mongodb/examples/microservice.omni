// Example: Microservice with omni-mongodb
import { HTTPServer } from '@omni/omni-mongodb/network'
import { DomainService } from '@omni/omni-mongodb/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}