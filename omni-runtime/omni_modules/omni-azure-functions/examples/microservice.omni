// Example: Microservice with omni-azure-functions
import { HTTPServer } from '@omni/omni-azure-functions/network'
import { DomainService } from '@omni/omni-azure-functions/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}