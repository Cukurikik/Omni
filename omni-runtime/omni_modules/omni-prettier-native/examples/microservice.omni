// Example: Microservice with omni-prettier-native
import { HTTPServer } from '@omni/omni-prettier-native/network'
import { DomainService } from '@omni/omni-prettier-native/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}