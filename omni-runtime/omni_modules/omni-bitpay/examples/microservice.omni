// Example: Microservice with omni-bitpay
import { HTTPServer } from '@omni/omni-bitpay/network'
import { DomainService } from '@omni/omni-bitpay/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}