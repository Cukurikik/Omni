// Example: Microservice with omni-tencent-cos
import { HTTPServer } from '@omni/omni-tencent-cos/network'
import { DomainService } from '@omni/omni-tencent-cos/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}