// Example: Microservice with omni-date-fns
import { HTTPServer } from '@omni/omni-date-fns/network'
import { DomainService } from '@omni/omni-date-fns/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}