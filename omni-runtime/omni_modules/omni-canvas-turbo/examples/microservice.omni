// Example: Microservice with omni-canvas-turbo
import { HTTPServer } from '@omni/omni-canvas-turbo/network'
import { DomainService } from '@omni/omni-canvas-turbo/domain'

fn main() {
    let svc = DomainService::new()
    let server = HTTPServer::new(3000)
    server.handle('/api/v1/entities', svc.handler())
    server.handle('/health', server.health_check())
    server.start()
}